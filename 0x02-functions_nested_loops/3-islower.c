#include "main.h"
/**
 * _islower - function
 *
 * @c: The character to evaluate
 *
 * Return: Get 1 if c is uppercase.
 * Get 0 otherwise
 */
int _islower(int c)
{
	if (c is >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
